package com.yenaly.han1meviewer.ios

/** Temporary iOS bootstrap exported to the native shell during the staged UI migration. */
object IosBootstrap {
    fun status(): String = "Han1meViewer iOS bootstrap is ready"
}
